import { AwsCredentialIdentityProvider } from '@aws-sdk/types';
import { ClientConfigFormat } from './client-config-types/client_config.js';
import { DeployedBackendIdentifier } from '@aws-amplify/deployed-backend-client';
/**
 * Main entry point for generating client config and writing to a file
 */
export declare const generateClientConfigToFile: (credentialProvider: AwsCredentialIdentityProvider, backendIdentifier: DeployedBackendIdentifier, outDir?: string, format?: ClientConfigFormat, log?: ((message: string) => void) | undefined) => Promise<void>;
//# sourceMappingURL=generate_client_config_to_file.d.ts.map